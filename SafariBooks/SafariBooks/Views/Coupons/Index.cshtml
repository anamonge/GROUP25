@model IEnumerable<SafariBooks.Models.Coupons>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CouponTypes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PromotionDetails)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Couponvalue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CouponTypes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PromotionDetails)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Couponvalue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CouponsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CouponsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CouponsID })
        </td>
    </tr>
}

</table>
